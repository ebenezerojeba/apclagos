import { ArticleSkeleton } from "@/components/ui/skeletons";

/** An event page shares the article shape: masthead, cover, body, aside. */
export default function Loading() {
  return <ArticleSkeleton />;
}
