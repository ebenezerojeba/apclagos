import { ProfileSkeleton } from "@/components/ui/skeletons";

/** Mirrors a leader profile: portrait and key-information column beside the biography. */
export default function Loading() {
  return <ProfileSkeleton />;
}
