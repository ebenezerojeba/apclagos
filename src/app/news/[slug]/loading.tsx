import { ArticleSkeleton } from "@/components/ui/skeletons";

/** Mirrors an article: dark masthead, lead image, prose column, share rail. */
export default function Loading() {
  return <ArticleSkeleton />;
}
