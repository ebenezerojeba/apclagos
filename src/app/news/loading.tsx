import { NewsroomSkeleton } from "@/components/ui/skeletons";

/**
 * Mirrors the newsroom: filters, a two-up article grid and the sidebar rails.
 */
export default function Loading() {
  return <NewsroomSkeleton />;
}
